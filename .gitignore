# utility stuff
venv/*
__pycache__/*

# ignore config so that as you put here sensitive data, you'll not accidentally commit it (but you'd better still be careful)
# it doesn't work though, I'm using git update-index --assume-unchanged config.py
config.py